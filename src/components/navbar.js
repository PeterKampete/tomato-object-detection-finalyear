import React from "react";
import { NavbarContainer } from "../styles";
const Navbar = () => {
  return (
    <NavbarContainer>
      <h2>DASHBOARD</h2>
    </NavbarContainer>
  );
};

export default Navbar;
